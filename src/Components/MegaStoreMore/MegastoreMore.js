import React from 'react';

const MegastoreMore = (props) => {
    const{name} = props.megaProduct
    return (
        <div>
            
            <h1>{name}</h1>
            
        </div>
    );
};

export default MegastoreMore;